import React, { Component } from 'react';

class BackButton extends Component{
    render(){
        return(
            <button class="backButton" a href="">Back</button>
        );
    }
}
export default BackButton;